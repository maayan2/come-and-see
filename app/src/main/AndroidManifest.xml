<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comeandsee">
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComeAndSee">
        <activity android:name=".Pop"/>
        <activity android:name=".AnimalidActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".VolunteeringActivity" />
        <activity android:name=".OrnithologyActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".FirstaidActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>